{
  "name": "graph-explorer-subgraph",
  "version": "0.1.0",
  "repository": "https://github.com/graphprotocol/explorer-dapp",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy-ropsten": "graph deploy graphprotocol/the-graph-ropsten --ipfs https://api.thegraph.com/ipfs/ --node https://api.thegraph.com/deploy/",
    "deploy-testing": "graph deploy davekaj/the-graph --ipfs https://api.thegraph.com/ipfs/ --node https://api.thegraph.com/deploy/",
    "deploy-mainnet": "graph deploy graphprotocol/the-graph --ipfs https://api.thegraph.com/ipfs/ --node https://api.thegraph.com/deploy/",
    "lint": "yarn eslint.",
    "lint:fix": "eslint . --fix",
    "prettier": "yarn prettier:js",
    "prettier:write": "prettier --write 'src/**/*.js'"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.18.0",
    "@graphprotocol/graph-ts": "^0.18.1",
    "prettier": "^1.19.1",
    "eslint": "^6.8.0"

  }
}
